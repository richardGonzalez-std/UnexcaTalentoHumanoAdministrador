# Login — Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Autenticar empleados por cédula + contraseña, emitiendo un JWT en cookie httpOnly, y entregar la pantalla de login (diseño Propuesta 4) en el frontend.

**Architecture:** Monolito modular (Spring Modulith), sin dependencias de login externo (sin OAuth2). Backend Spring Boot 4 con capas Controller → Service → Repository; contraseñas con BCrypt; JWT HS256 firmado/verificado con Nimbus (declarado explícito en build.gradle) guardado en cookie httpOnly; un filtro lee la cookie y autentica cada petición. Frontend Next.js 16: un Client Component `LoginForm` (estilos portados del mockup v4) que llama al backend con `credentials: 'include'`.

**Tech Stack:** Java 26, Spring Boot 4.0.6, Spring Security 7 (base, sin oauth2), Nimbus JOSE+JWT 10.4, BCrypt, MySQL; Next.js 16 (App Router), TypeScript, CSS Modules.

**Spec:** `docs/superpowers/specs/2026-05-31-login-design.md`
**Diseño aprobado:** `docs/design-companion/login-unexca-v4/index.html`

---

## Estructura de archivos

**Backend** (`back/src/main/java/com/unexca/talentohumano/`):
- `empleados/EmpleadoRepository.java` — *modificar*: añadir `findByCedula`.
- `config/SecurityConfig.java` — *modificar*: reglas + filtro JWT + CORS.
- `config/AppBeans.java` — *crear*: bean `PasswordEncoder` (BCrypt).
- `login/JwtService.java` — *crear*: emitir/validar JWT (Nimbus HS256).
- `login/JwtCookieFilter.java` — *crear*: lee cookie `token`, autentica.
- `login/LoginRequest.java`, `login/LoginResponse.java` — *crear*: DTOs (records).
- `login/CredencialesInvalidasException.java` — *crear*.
- `login/LoginService.java` — *crear*: valida credenciales (reutiliza `EmpleadoRepository`).
- `login/LoginController.java` — *crear*: `POST /api/auth/login`, `POST /api/auth/logout`.
- `config/GlobalExceptionHandler.java` — *crear*: excepción → HTTP.
- `dev/DemoSeeder.java` — *crear*: empleado de prueba (condicional).
- Tests: `login/JwtServiceTest.java`, `login/LoginServiceTest.java`.

**Frontend** (`front/`):
- `.env.local` — *crear*: `NEXT_PUBLIC_API_URL`.
- `src/app/login/types/auth.ts` — *crear*: tipos.
- `src/app/login/libs/api.ts` — *crear*: wrapper de login.
- `src/app/login/_components/LoginForm.tsx` — *crear*: el formulario (Client Component).
- `src/app/login/_components/LoginForm.module.css` — *crear*: estilos (port de v4).
- `src/app/login/page.tsx` — *modificar*: renderiza `<LoginForm/>`.

---

## Task 0: Dependencias ✅ (HECHO)

> **Estado:** completado en sesión. El bloque `dependencies` quedó así:
> - **Quitado** `spring-boot-starter-security-oauth2-client` (+ su `-test`) — OAuth2 es para
>   login con terceros (Google, etc.); esto es un monolito modular con login propio.
> - **Añadido** `spring-boot-starter-security` (seguridad base: filtros, SecurityContext, BCrypt).
> - **Añadido** `spring-boot-starter-validation` (para `@Email`/`@NotBlank` en runtime).
> - **Añadido** `com.nimbusds:nimbus-jose-jwt:10.4` explícito (lo arrastraba oauth2-jose; el JWT lo necesita).
> - **Añadido** `spring-boot-starter-test` + `spring-security-test` (testing).

**Files:** Modify: `back/build.gradle` (bloque `dependencies`)

- [x] **Step 1: Swap de dependencias** (ver bloque arriba)
- [x] **Step 2: Verificado** — `cd back && ./gradlew compileJava` → `BUILD SUCCESSFUL`
- [ ] **Step 3: Commit** (pendiente, junto con el resto del avance de la sesión)

```bash
git add back/build.gradle
git commit -m "build: drop oauth2 client, use security base + nimbus for local JWT login"
```

---

## Task 1: EmpleadoRepository.findByCedula

**Files:** Modify: `back/src/main/java/com/unexca/talentohumano/empleados/EmpleadoRepository.java`

- [ ] **Step 1: Añadir el método derivado y el import de Optional**

Reemplaza el contenido por:

```java
package com.unexca.talentohumano.empleados;

import org.springframework.data.jpa.repository.JpaRepository;
import java.util.Optional;

public interface EmpleadoRepository extends JpaRepository<Empleado, Long> {
    Optional<Empleado> findByCedula(String cedula);
}
```

- [ ] **Step 2: Compilar**

Run: `cd back && ./gradlew compileJava`
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 3: Commit**

```bash
git add back/src/main/java/com/unexca/talentohumano/empleados/EmpleadoRepository.java
git commit -m "feat(empleados): add findByCedula to repository"
```

---

## Task 2: PasswordEncoder (BCrypt) + propiedades JWT

**Files:**
- Create: `back/src/main/java/com/unexca/talentohumano/config/AppBeans.java`
- Modify: `back/src/main/resources/application.properties`

- [ ] **Step 1: Crear el bean BCrypt**

```java
package com.unexca.talentohumano.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;

@Configuration
public class AppBeans {
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

- [ ] **Step 2: Añadir propiedades JWT al final de application.properties**

```properties
# Configuración de JWT (HS256). El secreto DEBE tener >= 32 caracteres.
app.jwt.secret=cambia-este-secreto-por-uno-largo-de-32-o-mas-bytes
app.jwt.exp-minutes=120
app.jwt.exp-minutes-recordarme=10080
# Sembrar empleado de prueba al arrancar (solo desarrollo)
app.seed-demo=true
```

- [ ] **Step 3: Compilar y commit**

Run: `cd back && ./gradlew compileJava` → Expected: `BUILD SUCCESSFUL`

```bash
git add back/src/main/java/com/unexca/talentohumano/config/AppBeans.java back/src/main/resources/application.properties
git commit -m "feat(config): add BCrypt PasswordEncoder and JWT properties"
```

---

## Task 3: JwtService (Nimbus HS256) — TDD

**Files:**
- Create: `back/src/main/java/com/unexca/talentohumano/login/JwtService.java`
- Test: `back/src/test/java/com/unexca/talentohumano/login/JwtServiceTest.java`

- [ ] **Step 1: Escribir el test que falla**

```java
package com.unexca.talentohumano.login;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class JwtServiceTest {

    private final JwtService jwt =
        new JwtService("secreto-de-prueba-con-mas-de-32-bytes-ok!!", 120, 10080);

    @Test
    void generaYValidaToken() {
        String token = jwt.generar("V-12345678", 1L, false);
        var claims = jwt.validar(token);
        assertTrue(claims.isPresent());
        assertEquals("V-12345678", claims.get().cedula());
        assertEquals(1L, claims.get().empleadoId());
    }

    @Test
    void tokenAlteradoEsInvalido() {
        String token = jwt.generar("V-12345678", 1L, false);
        assertTrue(jwt.validar(token + "x").isEmpty());
    }
}
```

- [ ] **Step 2: Ejecutar el test (debe fallar a compilar)**

Run: `cd back && ./gradlew test --tests "*JwtServiceTest"`
Expected: FAIL (JwtService no existe)

- [ ] **Step 3: Implementar JwtService**

```java
package com.unexca.talentohumano.login;

import com.nimbusds.jose.*;
import com.nimbusds.jose.crypto.MACSigner;
import com.nimbusds.jose.crypto.MACVerifier;
import com.nimbusds.jwt.JWTClaimsSet;
import com.nimbusds.jwt.SignedJWT;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;

import java.nio.charset.StandardCharsets;
import java.util.Date;
import java.util.Optional;

@Service
public class JwtService {

    public record Claims(String cedula, long empleadoId) {}

    private final byte[] secret;
    private final long expMinutes;
    private final long expMinutesRecordarme;

    public JwtService(
            @Value("${app.jwt.secret}") String secret,
            @Value("${app.jwt.exp-minutes}") long expMinutes,
            @Value("${app.jwt.exp-minutes-recordarme}") long expMinutesRecordarme) {
        this.secret = secret.getBytes(StandardCharsets.UTF_8);
        this.expMinutes = expMinutes;
        this.expMinutesRecordarme = expMinutesRecordarme;
    }

    /** Minutos de vida del token según "recordarme". Útil también para el Max-Age de la cookie. */
    public long expSegundos(boolean recordarme) {
        return (recordarme ? expMinutesRecordarme : expMinutes) * 60;
    }

    public String generar(String cedula, long empleadoId, boolean recordarme) {
        try {
            long now = new Date().getTime();
            JWTClaimsSet claims = new JWTClaimsSet.Builder()
                    .subject(cedula)
                    .claim("eid", empleadoId)
                    .issueTime(new Date(now))
                    .expirationTime(new Date(now + expSegundos(recordarme) * 1000))
                    .build();
            SignedJWT jwt = new SignedJWT(new JWSHeader(JWSAlgorithm.HS256), claims);
            jwt.sign(new MACSigner(secret));
            return jwt.serialize();
        } catch (JOSEException e) {
            throw new IllegalStateException("No se pudo firmar el JWT", e);
        }
    }

    public Optional<Claims> validar(String token) {
        try {
            SignedJWT jwt = SignedJWT.parse(token);
            if (!jwt.verify(new MACVerifier(secret))) return Optional.empty();
            JWTClaimsSet c = jwt.getJWTClaimsSet();
            if (c.getExpirationTime() == null || c.getExpirationTime().before(new Date())) {
                return Optional.empty();
            }
            return Optional.of(new Claims(c.getSubject(), c.getLongClaim("eid")));
        } catch (Exception e) {
            return Optional.empty();
        }
    }
}
```

- [ ] **Step 4: Ejecutar el test (debe pasar)**

Run: `cd back && ./gradlew test --tests "*JwtServiceTest"`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add back/src/main/java/com/unexca/talentohumano/login/JwtService.java back/src/test/java/com/unexca/talentohumano/login/JwtServiceTest.java
git commit -m "feat(login): add JwtService with Nimbus HS256"
```

---

## Task 4: DTOs y excepción de dominio

**Files:**
- Create: `back/src/main/java/com/unexca/talentohumano/login/LoginRequest.java`
- Create: `back/src/main/java/com/unexca/talentohumano/login/LoginResponse.java`
- Create: `back/src/main/java/com/unexca/talentohumano/login/CredencialesInvalidasException.java`

- [ ] **Step 1: LoginRequest (con validación)**

```java
package com.unexca.talentohumano.login;

import jakarta.validation.constraints.NotBlank;

public record LoginRequest(
        @NotBlank(message = "La cédula es obligatoria") String cedula,
        @NotBlank(message = "La contraseña es obligatoria") String password,
        boolean recordarme
) {}
```

- [ ] **Step 2: LoginResponse**

```java
package com.unexca.talentohumano.login;

public record LoginResponse(Long id, String nombreCompleto) {}
```

- [ ] **Step 3: Excepción de dominio**

```java
package com.unexca.talentohumano.login;

public class CredencialesInvalidasException extends RuntimeException {
    public CredencialesInvalidasException(String mensaje) {
        super(mensaje);
    }
}
```

- [ ] **Step 4: Compilar y commit**

Run: `cd back && ./gradlew compileJava` → Expected: `BUILD SUCCESSFUL`

```bash
git add back/src/main/java/com/unexca/talentohumano/login/LoginRequest.java back/src/main/java/com/unexca/talentohumano/login/LoginResponse.java back/src/main/java/com/unexca/talentohumano/login/CredencialesInvalidasException.java
git commit -m "feat(login): add DTOs and domain exception"
```

---

## Task 5: LoginService — TDD

**Files:**
- Create: `back/src/main/java/com/unexca/talentohumano/login/LoginService.java`
- Test: `back/src/test/java/com/unexca/talentohumano/login/LoginServiceTest.java`

- [ ] **Step 1: Escribir el test que falla**

```java
package com.unexca.talentohumano.login;

import com.unexca.talentohumano.empleados.Empleado;
import com.unexca.talentohumano.empleados.EmpleadoRepository;
import org.junit.jupiter.api.Test;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;

import java.util.Optional;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

class LoginServiceTest {

    private final EmpleadoRepository repo = mock(EmpleadoRepository.class);
    private final BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();
    private final LoginService service = new LoginService(repo, encoder);

    private Empleado empleadoConPassword(String plano) {
        Empleado e = new Empleado();
        e.setId(1L);
        e.setCedula("V-12345678");
        e.setPrimerNombre("Ana");
        e.setApellidoPaterno("López");
        e.setPasswordHash(encoder.encode(plano));
        e.setActivo(true);
        return e;
    }

    @Test
    void autenticaConCredencialesCorrectas() {
        when(repo.findByCedula("V-12345678")).thenReturn(Optional.of(empleadoConPassword("secreto")));
        Empleado e = service.autenticar("V-12345678", "secreto");
        assertEquals(1L, e.getId());
    }

    @Test
    void rechazaCedulaInexistente() {
        when(repo.findByCedula("V-0")).thenReturn(Optional.empty());
        assertThrows(CredencialesInvalidasException.class,
                () -> service.autenticar("V-0", "x"));
    }

    @Test
    void rechazaPasswordIncorrecta() {
        when(repo.findByCedula("V-12345678")).thenReturn(Optional.of(empleadoConPassword("secreto")));
        assertThrows(CredencialesInvalidasException.class,
                () -> service.autenticar("V-12345678", "malo"));
    }
}
```

- [ ] **Step 2: Ejecutar (debe fallar a compilar)**

Run: `cd back && ./gradlew test --tests "*LoginServiceTest"`
Expected: FAIL (LoginService no existe)

- [ ] **Step 3: Implementar LoginService**

```java
package com.unexca.talentohumano.login;

import com.unexca.talentohumano.empleados.Empleado;
import com.unexca.talentohumano.empleados.EmpleadoRepository;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Service;

@Service
public class LoginService {

    private final EmpleadoRepository empleadoRepository;
    private final PasswordEncoder passwordEncoder;

    public LoginService(EmpleadoRepository empleadoRepository, PasswordEncoder passwordEncoder) {
        this.empleadoRepository = empleadoRepository;
        this.passwordEncoder = passwordEncoder;
    }

    public Empleado autenticar(String cedula, String password) {
        return empleadoRepository.findByCedula(cedula)
                .filter(e -> passwordEncoder.matches(password, e.getPasswordHash()))
                .orElseThrow(() -> new CredencialesInvalidasException(
                        "Cédula o contraseña incorrectas"));
    }
}
```

- [ ] **Step 4: Ejecutar (debe pasar)**

Run: `cd back && ./gradlew test --tests "*LoginServiceTest"`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add back/src/main/java/com/unexca/talentohumano/login/LoginService.java back/src/test/java/com/unexca/talentohumano/login/LoginServiceTest.java
git commit -m "feat(login): add LoginService with BCrypt verification"
```

---

## Task 6: Manejador global de excepciones

**Files:** Create: `back/src/main/java/com/unexca/talentohumano/config/GlobalExceptionHandler.java`

- [ ] **Step 1: Crear el advice**

```java
package com.unexca.talentohumano.config;

import com.unexca.talentohumano.login.CredencialesInvalidasException;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

import java.util.Map;

@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(CredencialesInvalidasException.class)
    public ResponseEntity<Map<String, String>> credenciales(CredencialesInvalidasException ex) {
        return ResponseEntity.status(HttpStatus.UNAUTHORIZED)
                .body(Map.of("mensaje", ex.getMessage()));
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<Map<String, String>> validacion(MethodArgumentNotValidException ex) {
        String msg = ex.getBindingResult().getFieldErrors().stream()
                .findFirst().map(f -> f.getDefaultMessage()).orElse("Datos inválidos");
        return ResponseEntity.badRequest().body(Map.of("mensaje", msg));
    }
}
```

- [ ] **Step 2: Compilar y commit**

Run: `cd back && ./gradlew compileJava` → Expected: `BUILD SUCCESSFUL`

```bash
git add back/src/main/java/com/unexca/talentohumano/config/GlobalExceptionHandler.java
git commit -m "feat(config): add global exception handler"
```

---

## Task 7: LoginController (cookie httpOnly)

**Files:** Create: `back/src/main/java/com/unexca/talentohumano/login/LoginController.java`

- [ ] **Step 1: Crear el controlador**

```java
package com.unexca.talentohumano.login;

import com.unexca.talentohumano.empleados.Empleado;
import jakarta.validation.Valid;
import org.springframework.http.HttpHeaders;
import org.springframework.http.ResponseCookie;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/auth")
public class LoginController {

    private static final String COOKIE = "token";

    private final LoginService loginService;
    private final JwtService jwtService;

    public LoginController(LoginService loginService, JwtService jwtService) {
        this.loginService = loginService;
        this.jwtService = jwtService;
    }

    @PostMapping("/login")
    public ResponseEntity<LoginResponse> login(@Valid @RequestBody LoginRequest req) {
        Empleado e = loginService.autenticar(req.cedula(), req.password());
        String token = jwtService.generar(e.getCedula(), e.getId(), req.recordarme());

        ResponseCookie cookie = ResponseCookie.from(COOKIE, token)
                .httpOnly(true)
                .secure(false)          // poner true en producción (HTTPS)
                .sameSite("Lax")
                .path("/")
                .maxAge(req.recordarme() ? jwtService.expSegundos(true) : -1)
                .build();

        String nombre = (safe(e.getPrimerNombre()) + " " + safe(e.getApellidoPaterno())).trim();
        return ResponseEntity.ok()
                .header(HttpHeaders.SET_COOKIE, cookie.toString())
                .body(new LoginResponse(e.getId(), nombre));
    }

    @PostMapping("/logout")
    public ResponseEntity<Void> logout() {
        ResponseCookie cookie = ResponseCookie.from(COOKIE, "")
                .httpOnly(true).path("/").maxAge(0).sameSite("Lax").build();
        return ResponseEntity.noContent()
                .header(HttpHeaders.SET_COOKIE, cookie.toString())
                .build();
    }

    private static String safe(String s) { return s == null ? "" : s; }
}
```

- [ ] **Step 2: Compilar y commit**

Run: `cd back && ./gradlew compileJava` → Expected: `BUILD SUCCESSFUL`

```bash
git add back/src/main/java/com/unexca/talentohumano/login/LoginController.java
git commit -m "feat(login): add LoginController issuing httpOnly cookie"
```

---

## Task 8: Filtro JWT + SecurityConfig + CORS

**Files:**
- Create: `back/src/main/java/com/unexca/talentohumano/login/JwtCookieFilter.java`
- Modify: `back/src/main/java/com/unexca/talentohumano/config/SecurityConfig.java`

- [ ] **Step 1: Crear el filtro que lee la cookie**

```java
package com.unexca.talentohumano.login;

import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.Cookie;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.stereotype.Component;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;
import java.util.List;

@Component
public class JwtCookieFilter extends OncePerRequestFilter {

    private final JwtService jwtService;

    public JwtCookieFilter(JwtService jwtService) {
        this.jwtService = jwtService;
    }

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response,
                                    FilterChain chain) throws ServletException, IOException {
        Cookie[] cookies = request.getCookies();
        if (cookies != null && SecurityContextHolder.getContext().getAuthentication() == null) {
            for (Cookie c : cookies) {
                if ("token".equals(c.getName())) {
                    jwtService.validar(c.getValue()).ifPresent(claims -> {
                        var auth = new UsernamePasswordAuthenticationToken(
                                claims.cedula(), null, List.of());
                        SecurityContextHolder.getContext().setAuthentication(auth);
                    });
                    break;
                }
            }
        }
        chain.doFilter(request, response);
    }
}
```

- [ ] **Step 2: Reemplazar SecurityConfig**

```java
package com.unexca.talentohumano.config;

import com.unexca.talentohumano.login.JwtCookieFilter;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;
import org.springframework.web.cors.CorsConfiguration;
import org.springframework.web.cors.CorsConfigurationSource;
import org.springframework.web.cors.UrlBasedCorsConfigurationSource;

import java.util.List;

@Configuration
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http, JwtCookieFilter jwtCookieFilter) throws Exception {
        http
            .cors(cors -> {})
            .csrf(csrf -> csrf.disable())
            .sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**",
                                 "/v3/api-docs/**", "/swagger-ui/**", "/swagger-ui.html").permitAll()
                .anyRequest().authenticated())
            .addFilterBefore(jwtCookieFilter, UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }

    @Bean
    public CorsConfigurationSource corsConfigurationSource() {
        CorsConfiguration c = new CorsConfiguration();
        c.setAllowedOrigins(List.of("http://localhost:3000"));
        c.setAllowedMethods(List.of("GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"));
        c.setAllowedHeaders(List.of("*"));
        c.setAllowCredentials(true);
        UrlBasedCorsConfigurationSource src = new UrlBasedCorsConfigurationSource();
        src.registerCorsConfiguration("/**", c);
        return src;
    }
}
```

- [ ] **Step 3: Compilar y commit**

Run: `cd back && ./gradlew compileJava` → Expected: `BUILD SUCCESSFUL`

```bash
git add back/src/main/java/com/unexca/talentohumano/login/JwtCookieFilter.java back/src/main/java/com/unexca/talentohumano/config/SecurityConfig.java
git commit -m "feat(login): JWT cookie filter, security rules and CORS"
```

---

## Task 9: Empleado de prueba (seed dev)

**Files:** Create: `back/src/main/java/com/unexca/talentohumano/dev/DemoSeeder.java`

- [ ] **Step 1: Crear el seeder condicional**

```java
package com.unexca.talentohumano.dev;

import com.unexca.talentohumano.empleados.Empleado;
import com.unexca.talentohumano.empleados.EmpleadoRepository;
import org.springframework.boot.CommandLineRunner;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Component;

@Component
@ConditionalOnProperty(name = "app.seed-demo", havingValue = "true")
public class DemoSeeder implements CommandLineRunner {

    private final EmpleadoRepository repo;
    private final PasswordEncoder encoder;

    public DemoSeeder(EmpleadoRepository repo, PasswordEncoder encoder) {
        this.repo = repo;
        this.encoder = encoder;
    }

    @Override
    public void run(String... args) {
        if (repo.findByCedula("V-12345678").isPresent()) return;
        Empleado e = new Empleado();
        e.setCedula("V-12345678");
        e.setPrimerNombre("Ana");
        e.setSegundoNombre("María");
        e.setApellidoPaterno("López");
        e.setApellidoMaterno("Pérez");
        e.setCorreo("ana.lopez@unexca.edu.ve");
        e.setTelefono("0414-1234567");
        e.setDireccion("Caracas");
        e.setPasswordHash(encoder.encode("Unexca2026"));
        e.setActivo(true);
        repo.save(e);
        System.out.println(">>> Empleado de prueba creado: V-12345678 / Unexca2026");
    }
}
```

- [ ] **Step 2: Compilar y commit**

Run: `cd back && ./gradlew compileJava` → Expected: `BUILD SUCCESSFUL`

```bash
git add back/src/main/java/com/unexca/talentohumano/dev/DemoSeeder.java
git commit -m "feat(dev): seed demo employee for login testing"
```

---

## Task 10: Verificación del backend (manual)

**Files:** none (verificación)

- [ ] **Step 1: Arrancar el backend (con MySQL corriendo)**

Run: `cd back && ./gradlew bootRun`
Expected en logs: `Empleado de prueba creado: V-12345678` y `Started TalentoHumanoApplication`.

- [ ] **Step 2: Login correcto → 200 + cookie**

Run:
```bash
curl -i -X POST http://localhost:8080/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"cedula":"V-12345678","password":"Unexca2026","recordarme":true}'
```
Expected: `HTTP/1.1 200`, cabecera `Set-Cookie: token=...; HttpOnly`, cuerpo `{"id":...,"nombreCompleto":"Ana López"}`.

- [ ] **Step 3: Credenciales incorrectas → 401**

Run:
```bash
curl -i -X POST http://localhost:8080/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"cedula":"V-12345678","password":"malo","recordarme":false}'
```
Expected: `HTTP/1.1 401`, cuerpo `{"mensaje":"Cédula o contraseña incorrectas"}`.

- [ ] **Step 4: Detener el backend** (Ctrl+C) y continuar con el frontend.

---

## Task 11: Frontend — entorno, tipos y API

**Files:**
- Create: `front/.env.local`
- Create: `front/src/app/login/types/auth.ts`
- Create: `front/src/app/login/libs/api.ts`

- [ ] **Step 1: Variable de entorno**

`front/.env.local`:
```
NEXT_PUBLIC_API_URL=http://localhost:8080
```

- [ ] **Step 2: Tipos** (`front/src/app/login/types/auth.ts`)

```ts
export interface LoginRequest {
  cedula: string;
  password: string;
  recordarme: boolean;
}

export interface LoginResponse {
  id: number;
  nombreCompleto: string;
}
```

- [ ] **Step 3: Wrapper de API** (`front/src/app/login/libs/api.ts`)

```ts
import type { LoginRequest, LoginResponse } from "../types/auth";

const API = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8080";

export async function login(req: LoginRequest): Promise<LoginResponse> {
  const res = await fetch(`${API}/api/auth/login`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    credentials: "include", // necesario para que el navegador acepte/envíe la cookie
    body: JSON.stringify(req),
  });

  if (!res.ok) {
    let mensaje = "No se pudo iniciar sesión";
    try {
      const data = await res.json();
      if (data?.mensaje) mensaje = data.mensaje;
    } catch { /* respuesta sin cuerpo JSON */ }
    throw new Error(mensaje);
  }
  return res.json();
}
```

- [ ] **Step 4: Borrar el `.gitkeep` de types y libs, y commit**

```bash
rm -f front/src/app/login/types/.gitkeep front/src/app/login/libs/.gitkeep
git add front/src/app/login/types/auth.ts front/src/app/login/libs/api.ts front/.gitignore 2>/dev/null
git add front/src/app/login/types front/src/app/login/libs
git commit -m "feat(front/login): env, auth types and login api wrapper"
```
> Nota: confirma que `.env.local` esté en el `.gitignore` (Next.js lo ignora por defecto); no lo subas.

---

## Task 12: Estilos del formulario (port de la Propuesta 4)

**Files:** Create: `front/src/app/login/_components/LoginForm.module.css`

- [ ] **Step 1: Crear el CSS module copiando los estilos de v4**

Copia el contenido del bloque `<style>` de `docs/design-companion/login-unexca-v4/index.html` (las reglas de `:root`, `body`, `.shell`, `.logo`, `.card`, `.head`, `.kicker`, `.title`, `.sub`, `form`, `.field`, `.control`, `.toggle`, `.ico-*`, `.row`, `.check`, `.box`, `.btn`, `.spin`, `.error`) a este archivo, con DOS ajustes:

1. Las variables de `:root` muévelas a un selector `.page { ... }` (en módulos, `:root` no se scopea). Aplica `.page` al contenedor raíz del componente.
2. Elimina la regla `.demo-badge` (era solo del mockup) y la regla `body{...}` cámbiala por `.page{...}` con las mismas propiedades de fondo/centrado (`min-height:100vh; display:grid; place-items:center; padding:28px; position:relative; background:var(--navy);`) y mantén el `::before` del degradado como `.page::before`.

> El import de la fuente Tektur va en el layout (Task 14, Step 1), no en el CSS module.

- [ ] **Step 2: Commit**

```bash
git add front/src/app/login/_components/LoginForm.module.css
git commit -m "feat(front/login): port v4 design to CSS module"
```

---

## Task 13: LoginForm (Client Component)

**Files:** Create: `front/src/app/login/_components/LoginForm.tsx`

- [ ] **Step 1: Crear el componente**

```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { login } from "../libs/api";
import styles from "./LoginForm.module.css";

export default function LoginForm() {
  const router = useRouter();
  const [cedula, setCedula] = useState("");
  const [password, setPassword] = useState("");
  const [recordarme, setRecordarme] = useState(false);
  const [mostrar, setMostrar] = useState(false);
  const [cargando, setCargando] = useState(false);
  const [error, setError] = useState<string | null>(null);

  async function onSubmit(e: React.FormEvent) {
    e.preventDefault();
    if (!cedula.trim() || !password.trim()) return;
    setError(null);
    setCargando(true);
    try {
      await login({ cedula, password, recordarme });
      router.push("/"); // destino placeholder; cambiar a /dashboard cuando exista
    } catch (err) {
      setError(err instanceof Error ? err.message : "No se pudo iniciar sesión");
    } finally {
      setCargando(false);
    }
  }

  return (
    <main className={styles.page}>
      <div className={styles.shell}>
        <div className={styles.logo}>
          {/* logo blanco desde front/public/logo.png */}
          <img src="/logo.png" alt="UNEXCA" />
        </div>

        <div className={styles.card}>
          <div className={styles.head}>
            <div className={styles.kicker}>Talento Humano · UNEXCA</div>
            <h1 className={styles.title}>Iniciar sesión</h1>
            <p className={styles.sub}>Ingresa con tu cédula y contraseña.</p>
          </div>

          <form onSubmit={onSubmit} noValidate>
            <div className={styles.field}>
              <label htmlFor="cedula">Cédula</label>
              <div className={styles.control}>
                <input
                  id="cedula"
                  type="text"
                  placeholder="V-12345678"
                  autoComplete="username"
                  value={cedula}
                  onChange={(e) => setCedula(e.target.value)}
                  disabled={cargando}
                />
              </div>
            </div>

            <div className={styles.field}>
              <label htmlFor="password">Contraseña</label>
              <div className={styles.control}>
                <input
                  id="password"
                  className={styles.pwInput}
                  type={mostrar ? "text" : "password"}
                  placeholder="••••••••••"
                  autoComplete="current-password"
                  value={password}
                  onChange={(e) => setPassword(e.target.value)}
                  disabled={cargando}
                />
                <button
                  type="button"
                  className={`${styles.toggle} ${mostrar ? styles.on : ""}`}
                  aria-label={mostrar ? "Ocultar contraseña" : "Mostrar contraseña"}
                  onClick={() => setMostrar((v) => !v)}
                >
                  <svg className={styles.icoOn} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8"><path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7-10-7-10-7Z"/><circle cx="12" cy="12" r="3"/></svg>
                  <svg className={styles.icoOff} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8"><path d="M3 3l18 18M10.6 5.1A9.8 9.8 0 0 1 12 5c6.5 0 10 7 10 7a17 17 0 0 1-3.3 4M6.6 6.6A17 17 0 0 0 2 12s3.5 7 10 7a9.7 9.7 0 0 0 4.2-.95M9.9 9.9a3 3 0 0 0 4.2 4.2"/></svg>
                </button>
              </div>
            </div>

            <div className={styles.row}>
              <label className={styles.check}>
                <input
                  type="checkbox"
                  checked={recordarme}
                  onChange={(e) => setRecordarme(e.target.checked)}
                  disabled={cargando}
                />
                <span className={styles.box}>
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="3.4"><path d="M20 6 9 17l-5-5"/></svg>
                </span>
                Recordarme
              </label>
            </div>

            <button type="submit" className={`${styles.btn} ${cargando ? styles.loading : ""}`} disabled={cargando}>
              <span className={styles.spin}></span>
              <span className={styles.btnLabel}>Ingresar</span>
            </button>

            {error && (
              <div className={`${styles.error} ${styles.show}`}>
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" style={{ width: 17, height: 17, flex: "none" }}><circle cx="12" cy="12" r="9"/><path d="M12 8v5M12 16h.01"/></svg>
                {error}
              </div>
            )}
          </form>
        </div>
      </div>
    </main>
  );
}
```

> **Importante sobre los nombres de clase:** en el CSS module, las clases con guion del mockup (`.pw-input`, `.ico-on`, `.ico-off`, `.btn-label`) deben renombrarse a camelCase (`.pwInput`, `.icoOn`, `.icoOff`, `.btnLabel`) para usarlas como `styles.pwInput` etc. Renómbralas en `LoginForm.module.css` al hacer el Task 12. La regla `.toggle.on .ico-on` queda `.toggle.on .icoOn`, etc.

- [ ] **Step 2: Borrar `.gitkeep` de _components y commit**

```bash
rm -f front/src/app/login/_components/.gitkeep
git add front/src/app/login/_components/LoginForm.tsx
git commit -m "feat(front/login): LoginForm client component"
```

---

## Task 14: Cablear la página

**Files:** Modify: `front/src/app/login/page.tsx`

- [ ] **Step 1: Renderizar el formulario + cargar la fuente Tektur**

```tsx
import type { Metadata } from "next";
import LoginForm from "./_components/LoginForm";

export const metadata: Metadata = {
  title: "Iniciar sesión · Talento Humano UNEXCA",
};

export default function LoginPage() {
  return (
    <>
      {/* Fuente Tektur (diseño Propuesta 4) */}
      <link
        href="https://fonts.googleapis.com/css2?family=Tektur:wght@400;500;600;700&display=swap"
        rel="stylesheet"
      />
      <LoginForm />
    </>
  );
}
```

- [ ] **Step 2: Compilar el front**

Run: `cd front && npm run build`
Expected: build sin errores de TypeScript.

- [ ] **Step 3: Commit**

```bash
git add front/src/app/login/page.tsx
git commit -m "feat(front/login): wire LoginForm into /login page"
```

---

## Task 15: Verificación end-to-end (manual)

**Files:** none

- [ ] **Step 1: Arrancar backend y frontend**

Terminal A: `cd back && ./gradlew bootRun`
Terminal B: `cd front && npm run dev`

- [ ] **Step 2: Probar en el navegador**

Abrir `http://localhost:3000/login`. Verificar:
- Se ve el diseño Propuesta 4 (tarjeta blanca, logo blanco arriba, fuente Tektur).
- El ojo muestra/oculta la contraseña.
- Con `V-12345678 / Unexca2026` → redirige a `/`.
- Con contraseña incorrecta → aparece "Cédula o contraseña incorrectas".

- [ ] **Step 3: Verificar la cookie**

En DevTools → Application → Cookies → `http://localhost:8080`, confirmar que existe la cookie `token` marcada **HttpOnly**.

- [ ] **Step 4: Commit final (si hubo ajustes)**

```bash
git add -A && git commit -m "chore(login): end-to-end verified"
```

---

## Notas de cierre

- **Destino post-login** (`router.push("/")`) es un placeholder; cambiar a `/dashboard` cuando exista (spec §10).
- **Producción:** poner `secure(true)` en la cookie (HTTPS) y revisar `SameSite` si front y back quedan en dominios distintos.
- **Validación @Email/@NotBlank** ya funciona en runtime gracias a `spring-boot-starter-validation` (Task 0).
